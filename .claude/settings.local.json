{
  "permissions": {
    "allow": [
      "Bash(xcodebuild -project OpenHood.xcodeproj -scheme OpenHood -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -configuration Debug build)"
    ]
  }
}
